# Servidor
PORT=3000
NODE_ENV=development

# MySQL (no subir valores reales)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=suldery_nails

# Generar con un gestor de secretos: minimo 32 caracteres aleatorios
JWT_SECRET=replace_with_a_random_secret_of_at_least_32_characters

# En produccion, lista exacta y separada por comas de origenes permitidos
CORS_ORIGIN=https://tu-dominio.com